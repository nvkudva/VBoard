# Pin the emulated SDK so JVM tests do not silently change behaviour when
# compileSdk/targetSdk move. 34 over 35 deliberately: it is the level Robolectric
# 4.14.x has the most mileage on, and nothing under test here is API 35 specific.
sdk=34
