<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Held only while a model download is in flight, so a screen-off transfer
         isn't killed by the socket read timeout partway through ~480MB. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".VBoardApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.VBoard">

        <service
            android:name=".ime.VBoardImeService"
            android:exported="true"
            android:label="@string/ime_name"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- The refiner runs in its own process so a native OOM or a MediaPipe
             crash inside a 0.5B model cannot take the keyboard down mid-sentence,
             and so the keyboard's memory ceiling stops being set by an optional
             feature (V2_PLAN Wave 0.5). Not exported: the only client is us. -->
        <service
            android:name=".llm.LlmRefinerService"
            android:exported="false"
            android:process=":llm" />

        <!-- WorkManager is initialized on demand (VBoardApp implements
             Configuration.Provider) instead of by androidx.startup, so the
             keyboard process does not host the downloader's machinery. Removing
             just this initializer leaves the startup provider available for
             anything else that needs it. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Model downloads run as a WorkManager long-running worker (see
             ModelDownloadWorker), which hosts its foreground notification in WorkManager's
             own service. That service ships without a foregroundServiceType, and targetSdk
             34+ refuses to start an untyped foreground service, so the type is merged in
             here. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            android:process=":ui"
            tools:node="merge" />

        <activity
            android:name=".onboarding.OnboardingActivity"
            android:exported="true"
            android:process=":ui"
            android:theme="@style/Theme.VBoard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".settings.SettingsActivity"
            android:exported="true"
            android:label="@string/settings_title"
            android:process=":ui"
            android:theme="@style/Theme.VBoard"
            tools:ignore="ExportedActivity" />
    </application>
</manifest>
